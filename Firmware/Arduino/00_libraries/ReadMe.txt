Place these folders with the other libraries for Arduino. After doing this, updating libraries in Arduino may update the lvgl library and break the code. make sure the version of the files in your Arduino Library folder matches these.

Windows:
C:\Users\[user_name]\Documents\Arduino\libraries